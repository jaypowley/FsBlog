@{
    Layout = "default";
    Title = "FsBlog";
    Description = "A blog aware static site generator in mostly F#";
}
@using System.Linq

@foreach (var post in Enumerable.Take(Model.Beers, 10))
{
    <a href="@post.Url/index.html">@post.Title</a><br />
}